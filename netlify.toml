# El sitio es estático (index.html). Las funciones manejan Mercado Pago.
[build]
  publish = "."
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

[functions]
  node_bundler = "esbuild"
